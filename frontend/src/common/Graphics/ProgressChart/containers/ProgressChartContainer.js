import React from "react";

import ProgressChart from "../components";

const ProgressChartContainer = ({ title }) => {
  return <ProgressChart title={title} />;
};

export default ProgressChartContainer;
