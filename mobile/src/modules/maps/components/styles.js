import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mapsWrapper: {
    flex: 1,
    alignItems: 'center',
    paddingTop: 20,
  },
  image: {
    width: null,
    height: null,
    flex: 1,
  },
});

export default styles;
